![alt text](image-17.png)
데이터의 피처 특징 파악과 순서 확인
![alt text](image-18.png)
데이터 수치적인 특징 확인 수치의 스케일이 다양하게 나타나니 정규화해줘야 함을 확인
![alt text](image-19.png)
정규화로 전처리 완료
![alt text](image-20.png)
모델정의
![alt text](image-21.png)
학습 설정와 50에포크 모델 학습
![alt text](image-22.png)
로스가 감소하는 걸 확인했고 테스트셋으로 평가하니 MSE는 0.3282가 나왔다. MAE는 0.4023이고 실제 금액은 $40,230이다.
![alt text](image-23.png)
모델 가중치 저장과 전처리 파라미터를 같이 저장하였다.
전처리된 데이터로 가중치를 저장하였기 때문에 동일한 전처리 파라미터를 사용해야 모델이 제 성능을 발휘할 수 있다. 데이터 누구 방지와 학습/테스트/배포 환경의 전처리 일관성을 확보한다.
![alt text](image-24.png)
모델 가중치(.pth)와 전처리 파라미터(`train_mean`, `train_std`) 그리고 모델의 추론 함수 역시도 포함되어야 해서 모듈화 해준다.
![alt text](image-26.png)
![alt text](image-25.png)
스키마 생성과 FASTAPI 서버 구현시 입력해야하는 피처들과 field를 적절히 지정하여 에러를 방지한다.
입력 변수 8가지와 ge,gt,le 등 config는 예제값을 입력해 두었다.
![alt text](image-27.png)
hursing_aPi 로거와 모델과 전처리 파라미터 로드하고 run in executor 기능을 사용하기 위해 ThreadPoolExecutor로 모델 추론 작업을 처리할 전용 스레드풀을 만든다. lifespan() 함수는 서버가 시작될 때 HousingPredictor를 생성하여 모델과 전처리 정보를 한 번만 로드하게 한다. 요청이 올 때마다 모델을 다시 불러오지 않아도 되어 서버가 더 빠르고 안정적으로 동작함
FastAPI 앱을 만들고, 요청 로깅 미들웨어와 에러 핸들러를 등록.
![alt text](image-28.png)
/health 엔드포인트로 서버 상태 확인 (Get method사용)
/predict 엔드포인트로 주택정보를 예측집값으로 요청한다.HousingRequest 형식의 입력 데이터를 받고, 응답은 HousingResponse 형식으로 반환합니다. 모델이 아직 없을 때 503 반환해주고 Run in executor기능도 포함시켰다.
추론 중 예상하지 못한 오류가 발생하면 500 상태 코드와 함께 오류 메시지를 반환합니다.(예외처리)
예측이 성공하면 최종응답도 나옵니다.
![alt text](image-29.png)
서버실행과 헬스체크
![alt text](image-30.png)
요청성공과 에러 테스트
![alt text](image-31.png)
대시보드 코드 작성
모델 api 연결하지 못하거나 오류가 날 경우 예외처리 조항들
![alt text](image-32.png)
사이드바에 api 서버 연결 여부를 보여준다.
![alt text](image-33.png)
데이터입력 영역에서 앞서 작성한 스키마안 필드에 맞는 입력값 범위를 설정해준다.
![alt text](image-34.png)
예측 집값과 입력 주택정보를 확인하는 결과창을 작성하였다.
![alt text](image-35.png)
streamlit으로 대시보드를 실제로 포트에 뛰어준다.

![alt text](image-8.png)
테스트1
![alt text](image-9.png)
테스트2
![alt text](image-10.png)
가격 상승! 야호
![alt text](image-11.png)
가격 하락! 오
테스트3
![alt text](image-12.png)

테스트1
![alt text](image-13.png)
테스트2
![alt text](image-14.png)
테스트3
![alt text](image-15.png)
테스트4
![alt text](image-16.png)

![alt text](image-36.png)
저소득 고소득 평균주택을 지정해서 다양하게 요청해보기
![alt text](image-37.png)
다양한 에러상황을 발생시켜 보기
필드누락/범위초과/음수소득/잘못된 포맷
![alt text](image-38.png)
쓰레드풀 8개에 모델추론을 동시요청
![alt text](image-39.png)
헬프체크와 모든 테스트 결과

1. 이 프로젝트의 입력과 출력은 각각 무엇입니까? 집 주택 정보(면적, 방수, 위치 등) 입력하여 집값 정보를 예측합니다.
2. MNIST 프로젝트와 비교했을 때, 데이터 형태가 어떻게 다릅니까? 라벨링된 데이터셋을 전처리하여  pytorch 모델을 학습하여 예측 모델을 만들고 회귀 task를 수행합니다. 이전 데이터는 픽셀 파일을 사용하여 분류 task를 해결했습니다.MNIST 프로젝트는 28×28 픽셀 이미지 데이터를 입력으로 사용하고, 숫자 0~9 중 하나를 맞히는 분류 문제였습니다. 반면 이번 주택 가격 예측 프로젝트는 이미지가 아니라 여러 수치형 컬럼으로 이루어진 표 형식 데이터를 입력으로 사용하며, 집값이라는 연속적인 숫자를 예측하는 회귀 문제입니다.
3. 오늘 새로 만들 파일 3개의 이름과 역할을 말할 수 있습니까? model.py는 주택 가격 예측에 사용할 PyTorch 모델 구조를 정의하는 파일입니다.
predict.py는 학습된 모델 가중치를 불러오고, 입력 데이터를 받아 집값을 예측하는 추론 함수를 담는 파일입니다.
main.py는 FastAPI 서버 파일로, 사용자가 보낸 주택 정보를 API로 받아 predict.py의 추론 함수를 실행한 뒤 예측 결과를 응답합니다.
대시보드 파일은 사용자가 직접 값을 입력할 수 있는 UI를 제공하고, 입력값을 FastAPI 서버로 전송한 뒤 예측된 주택 가격을 화면에 보여주는 프론트엔드 역할을 합니다.

1. 정규화에서 학습 데이터의 통계를 테스트 데이터에도 사용하는 이유는 무엇입니까? 테스트 데이터는 "실제 배포 환경에서 들어올 새로운 데이터"를 시뮬레이션합니다. 데이터 누수 방지, 학습/테스트/배포 환경의 전처리 일관성 유지위해서 학습 데이터의 통계를 사용합니다.
2. 모델 가중치 외에 **함께 저장해야 하는 것**은 무엇이고, 왜 필요합니까? 모델 가중치뿐만 아니라 학습 데이터의 평균, 표준편차, 피처 이름과 순서 같은 전처리 정보를 함께 저장해야 합니다. 모델은 학습 때 정규화된 입력을 기준으로 가중치를 학습했기 때문에, 배포 시에도 같은 전처리 과정을 거쳐야 올바른 예측을 할 수 있습니다.
3. `HousingPredictor.predict()`에서 피처를 `self.feature_names` 순서로 배열하는 이유는? HousingPredictor.predict()에서 피처를 self.feature_names 순서로 배열하는 이유는 모델이 학습할 때 사용한 입력 컬럼 순서와 예측할 때의 입력 순서를 반드시 맞춰야 하기 때문입니다. PyTorch 모델은 컬럼 이름을 보고 판단하지 않고 숫자 배열의 위치를 기준으로 계산하므로, 순서가 바뀌면 완전히 잘못된 예측이 나올 수 있습니다.

1. `HousingRequest`에서 `Latitude`에 `ge=32, le=42` 제한을 넣은 이유는? 위도 수치를 넣어야 하고 갤리포니아 지역을 벗어나지 않기 위해서 제한을 넣었습니다. 또한 adc 같은 말이 안 되는 입력값을 API 단계에서 미리 막기 위해서입니다.
2. `request.model_dump()`는 어떤 역할을 합니까? request.model_dump()는 FastAPI가 받은 HousingRequest 객체를 일반 Python 딕셔너리로 변환하는 역할을 합니다. 이렇게 변환해야 predict() 함수에 입력값을 넘기거나, 피처 이름 기준으로 값을 꺼내서 배열로 만들기 쉽습니다.
3. `run_in_executor`를 사용하지 않으면 어떤 문제가 발생할 수 있습니까? 모델 추론 같은 동기 작업이 FastAPI의 이벤트 루프를 막을 수 있습니다. 그러면 한 사용자의 예측 요청을 처리하는 동안 다른 요청도 같이 지연되어 서버 응답성이 떨어질 수 있습니다. run_in_executor는 이런 추론 작업을 별도 스레드에서 실행해 서버가 다른 요청도 처리할 수 있게 해줍니다.

1.`st.number_input()`에서 `min_value`, `max_value`를 설정하는 이유는?
3. `reques MNIST 대시보드(Day 4)와 비교했을 때 입력 방식이 어떻게 다릅니까?
2. t_data` 딕셔너리의 키 이름이 `HousingRequest` 스키마의 필드 이름과 정확히 일치해야 하는 이유는?


Q1. 전처리 파라미터(mean, std)를 모델과 함께 저장해야 하는 이유는?
Q2. HousingRequest에서 Latitude에 ge=32, le=42를 넣은 이유는?
Q3. Streamlit의 입력값 이름이 Pydantic 스키마의 필드 이름과 일치해야 하는 이유는?
Q4. 이 프로젝트에서 run_in_executor를 제거하면 어떤 문제가 생길 수 있습니까?
Q5. MNIST 프로젝트(Day 1~4)와 오늘 프로젝트의 가장 큰 차이는 무엇입니까?

회고
백엔드 구축을 위해 모델을 학습과 데이터 전처리, 모델 가중치 저장과 전처리 파라미터 저장을 하면서 print()통헤 로깅을 하며 테스트하고 모듈화된 모델을 api서버를 올리기 위해서 FASTAPI와 서버를 구축하여 다시 테스트를 하는 과정에서 어느 부분에서 에러가 발생할지 알수 없기에 체크포인트로서 로깅과 테스트를 일관되게 해야 되는 것을 깨달았습니다. 노트북의 코드는 에러없이 잘 작동되었지만 실제 모델 배포를 하는 과정에서는 부드럽게 흐름이 이어지지 않을수 있기 때문에 모델 직렬화와 스키마 구축, FASTAPI로 서버구현과 paydantic구성, 대시보드까지 각 구성요소에서 구현화되어야 할 요소들을 체킹하면서 넘어가는게 중요하고 생각이 되었습니다.